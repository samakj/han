import React, { useState } from 'react';

import '@han/components/navigation/style.scss';
import { NavigationPropsType } from '@han/components/navigation/types';

export const Navigation: React.FunctionComponent<NavigationPropsType> = ({}) => {
    return (
        <nav className="main-navigation">
        </nav>
    );
};
